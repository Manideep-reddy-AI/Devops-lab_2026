<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom border-dark-subtle sticky-top">
  <div class="container-fluid px-3 px-lg-4">
    <button class="btn btn-outline-light d-lg-none me-2" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarOffcanvas" aria-controls="sidebarOffcanvas">
      Menu
    </button>
    <a class="navbar-brand fw-semibold" href="/dashboard"><%= appName %></a>

    <div class="ms-auto d-flex align-items-center gap-2">
      <% if (user) { %>
        <div class="text-white-50 d-none d-md-block">
          <div class="small">Signed in as</div>
          <div class="fw-semibold text-white"><%= user.name %> · <%= user.role %></div>
        </div>
        <a class="btn btn-outline-light btn-sm" href="/profile">Profile</a>
        <form action="/auth/logout" method="post" class="d-inline">
          <button class="btn btn-light btn-sm" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="btn btn-outline-light btn-sm" href="/auth/login">Login</a>
      <% } %>
    </div>
  </div>
</nav>
