{
  "name": "expenses",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^12.0.0"
  },
  "jest": {
    "reporters": [
      "default",
      [
        "jest-junit",
        {
          "output": "test-results.xml",
          "suiteName": "expenses-tests",
          "usePathForSuiteName": false
        }
      ]
    ]
  }
}
